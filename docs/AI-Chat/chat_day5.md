# 
基于 `ch4/exercise.md` 的要求，给我“最小可通过测例”的实现路线图。
要求：
1. 按文件、函数、改动点列出；
2. 每步说明输入/输出与预期日志；
3. 不一次性给全量代码，只给当前步需要的核心片段。

# ch4最小实现步骤

基于 ch4/exercise.md 的要求，给我“最小可通过测例”的实现路线图。
要求：

按文件、函数、改动点列出；
每步说明输入/输出与预期日志；
不一次性给全量代码，只给当前步需要的核心片段。

# ch4调试
这是当前报错与日志：
`一直在运行却没有输出，也不能结束`

请给出：
1. 最可能 3 个根因（按概率排序）；
2. 每个根因的验证步骤（最多 3 步）；
3. 最短修复路径；
4. 修复后如何防止回归。

