# question1
我想实现一个名为tg-rcore-tutorial-ch1-clock的内核，该内核功能如下：
1.定时输出字符。
这个内核是tg-rcore-tutorial内核的扩展。
要求：
1.跟据tg-rcore-tutorial-ch1的目录结构，生成tg-rcore-tutorial-ch1-clock的目录。
2.给出我该如何在tg-rcore-tutorial-ch1内核的基础上实现tg-rcore-tutorial-ch1-clock这个内核。

按要求生成一份计划书。

# res1
AI生成了一份计划书，计划书有的步骤不知道rust代码怎么写。

# question2
给出ch1-clock.md里每个步骤的rust代码该如何实现，并给出3-5个知道步骤的意思但是写不出来对应的rust代码的原因，并给出相应的解决办法，把生成的文档写入docs/plans/ch1-clock/ch1-clock-code.md。

# res2
AI给出了一个版本的代码写法和可能出现的困难，但我还是看不懂CSR相关的知识，这时，我发现我应该是不了解CSR的相关知识，并且AI给出的代码都是使用asm!通过伪指令操作寄存器，那为什么不通过rustsbi来操作呢？
先学习CSR相关知识，然后初步实现ch1-clock内核，然后再分析不同的方法。
学习链接：https://rcore-os.cn/rCore-Tutorial-Book-v3/chapter2/4trap-handling.html#term-s-mod-csr

# question3
编码完成后运行没有成功，可以正常编译运行，但是输出Time started!后，没有t字符输出，请给我生成一份bug排除方案，并把方案写入docs/plans/ch1-clock/debug01.md里。

# res3
按AI生成的方案排查发现，没有出发自己定义的trap_handler，询问原因。

# question4
发现trap handler没有被调用，给出原因和解决方案，并写入docs/plans/ch1-clock/debug02.md

# question5
在 `rust_main` 中添加调试输出检查寄存器值后，输出YYXXXXXXXXXXXXX，其中少的那个Y是mideleg的mideleg & 0x20==0，给出原因和解决方案，并写入docs/plans/ch1-clock/debug03.md